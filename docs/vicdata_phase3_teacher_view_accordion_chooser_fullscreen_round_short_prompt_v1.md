Read `docs/vicdata_phase3_teacher_view_accordion_chooser_fullscreen_round_build_brief_v1.md` and `docs/vicdata_phase3_teacher_view_accordion_chooser_fullscreen_round_claude_code_prompt_v1.md` in this repo, then build all 4 parts in order, one commit per part, plus a build report. Ground truth for file/line pointers was checked against HEAD `45fca0c` — re-read files before editing since line numbers may have shifted. Part 1 is a real bug fix (Comparisons Trend chart, zero height with many comparators); Part 2 is a direct instruction (accordion + height, no ambiguity); Parts 3 and 4 are explicit first drafts — get the mechanism working end to end and flag judgement calls in the report rather than guessing silently, especially the height constant in Part 2 and the layout split in Part 4.

This is the last round for tonight. After building all 4 parts and writing the report, stop — don't pick up anything else, including the content round's or round 2's own open decisions (§D1-§D7 / §D1-§D5), which are confirmed-as-built or deferred, not queued for tonight. The next step is one full live walkthrough tomorrow covering everything on `main`, not another build.
