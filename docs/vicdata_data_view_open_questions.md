# VicData — Member Data View build: decisions, findings, and open items

*Log kept live during the autonomous build session for `docs/vicdata_phase3_member_data_view_build_brief_v1.md`, per that brief's §0 protocol. "Decisions" are genuine ambiguous calls made and built on top of; "Findings" are bugs/data-quality issues/repo-state facts discovered along the way, distinct from decisions. Entries are added as they happen, not batched at the end.*

---

## Repo-check items (brief's own explicit pre-flight list)

**PDF-export pattern (§9)**: grepped the whole repo (`window.print`, `@media print`, `afterprint`) — **zero hits**. There is no existing print/export pattern anywhere in `vicdata_public`. The brief's §9 describes reusing "VicDash's existing pattern" (a separate Phase 2 repo not present here) — that pattern isn't available to inspect or port from directly in this repo; built fresh, matching the *described* shape (`window.print()` + `@media print`, `afterprint`-gated palette revert) since that's the best available reference. See §9's own build entry below for what was actually built and the branding check.

**`saved_sets.school_account_id` (§6.2)**: confirmed via `supabase/migrations/20260807093000_saved_sets.sql` — the column is genuinely `not null references public.school_accounts (id) on delete cascade`. The brief's premise is correct. See the full decision below.

**`phaseTags()`/`effectivePhaseTags()`/`findSurroundingSchools()` current state (§2)**: read directly before touching anything (not assumed from the brief's design-conversation framing). Two of the brief's four described bugs were **already fixed** in the current commit, one **partially** fixed, and the boarding-ratio anomaly **did not reproduce** against live data. Full detail under each numbered item below.

---

## §2 prerequisite fixes

### 1. Wire `effectivePhaseTags()` into the target's own phase (findSurroundingSchools)

**Confirmed a real, live bug** — `findSurroundingSchools()` (`src/lib/surrounding-schools.ts`) computed the *target's* own phase via raw `phaseTags()`, not `effectivePhaseTags()`. Fixed: the target's own real DfE census facts are now fetched (one small single-entity `lookupReferenceData` call, added before the `nearest_schools` RPC call since `relaxSectorForThroughSchool` depends on the target's phase-tag count) and `effectivePhaseTags()` is used instead.

**Verified against real data**: Woldingham (URN 125369) — nominal `["Junior","Senior"]` → effective `["Senior"]` (0 real pupils at age 10, 65 at age 11). Stockport Grammar (URN 106156) — nominal `["Junior","Senior"]` → effective `["Junior","Senior"]` (68 real pupils at age 10 — a genuine through-school, correctly unchanged). Confirmed end-to-end via `findSurroundingSchools`: Woldingham's own nearest-10 now includes a single-tag Senior-only candidate (Al-Khair Secondary Girls, ages 11-16) that the old nominal-tag bug would have wrongly excluded (see item 2 below for why).

Candidates deliberately still use raw `phaseTags()`, not `effectivePhaseTags()` — see item 2.

### 2. Through-school candidate-pool symmetry check

**Finding, not a new bug**: this fix was **already present** in the current commit (`src/lib/surrounding-schools.ts`'s phase-tag filter already required `candidatePhase.length > 1` whenever `targetPhase.length > 1`, per its own "2026-09-11, round 19, item 5 bug B" comment, citing the exact Stockport Grammar case the brief itself cites). The brief's description of this as an open bug reflects the design-conversation snapshot it was written from, not this repo's current state. No code change was needed here — confirmed correct and left as-is, and confirmed it composes correctly with item 1's fix: switching the target's own phase to `effectivePhaseTags()` means Woldingham now resolves to a single effective tag (Senior), so this symmetry check no longer applies to it at all (correct — Woldingham should match ordinary single-tag Senior schools too, not just other through-schools), while Stockport Grammar (a genuine through-school) keeps two effective tags and the symmetry check still narrows its pool correctly.

Per the brief's own instruction, candidates are kept on raw `phaseTags()` (not `effectivePhaseTags()`) deliberately — this module doesn't fetch per-candidate census data, so enrollment-aware tags aren't cheaply available for the ~40-candidate buffer. Carried forward as a decision, not re-litigated.

### 3. Nursery/PRU exclusion at source

**Finding**: partially fixed already, via a different, earlier round's mechanism than the brief describes. A prior session (this same build environment, immediately preceding this one) had already added an age-based cutoff to `phaseTags()` (`if (highAge <= 5) return []`) that correctly excludes nursery schools by age, confirmed against real data at the time (382 schools, 375 of them `establishment_type = 'Local authority nursery school'`). Separately, `nearest_schools`' own RPC (`supabase/migrations/20260930120000_nearest_schools_special_and_through_school.sql`) **already excludes PRUs at the SQL level** via `establishment_type not ilike '%referral%'` — a pre-existing filter, not something I added.

**What was genuinely still broken**: neither of those two existing fixes is a source-level `establishment_type` check inside `phaseTags()`/`sectorTag()` themselves, so **other consumers were still affected**:
- `schools-in-bounds/route.ts` (the public map's own bounding-box query) has **no** equivalent PRU exclusion at all — confirmed live in that file's own comment ("NOT applying nearest_schools' extra alternative-provision/referral substring exclusion here... Left as a known gap for now... worth a decision, not an assumption"). A PRU spanning ordinary primary ages (e.g. New River College Primary, ages 5-11) was rendering as a green "Junior"-tagged, "State"-coloured dot on the live public map.
- `sectorTag()` itself had **no** nursery/PRU exclusion at all (a stale comment in the same function already claimed "PRUs/AP -- expected fall-through", which was **not actually true** of the code — a real, pre-existing documentation/implementation mismatch, logged here as a finding in its own right).
- `narrative-lookup.ts`'s LA-peer-average computation (Topic 3's "large/medium/small for its phase" sentence, shown on every mainstream school's free public page) had no exclusion either — a PRU or nursery in the same LA/sector as a genuine target school was silently averaged in as a "peer," corrupting that sentence for every real primary/secondary school nationally whenever a PRU/nursery happened to sit in the same LA.

**Fix applied**: confirmed the exact two live GIAS `establishment_type` strings against real data (`Local authority nursery school`, `Pupil referral unit` — verified as the *only* two values matching case-insensitive "nursery"/"referral" respectively across the live `schools` table). Added a shared exclusion check to **both** `phaseTags()` and `sectorTag()` in `typology.ts` (returns `[]`/`null` respectively for either establishment type), per the brief's explicit "fix at source... every consumer gets it for free" instruction. This is a genuine signature change (`phaseTags`/`effectivePhaseTags` both gained a new `establishmentType` parameter) threaded through all 9 real call sites: `surrounding-schools.ts` (×2), `schools-in-bounds/route.ts` (×2), `narrative-lookup.ts` (×4, including the peer-loop's own SQL select gaining an `establishment_type` column), and `schools/[urn]/page.tsx` (×2, one direct `effectivePhaseTags` call and the `computeTopic3SizeSentence` call site).

**Verified against real data**: `findSurroundingSchools("100429", ...)` (Yerbury Primary School) — the real nearest-10 result contains zero of the four named schools (Margaret McMillan Nursery, Kate Greenaway Nursery, North Islington Nursery, New River College Primary), confirmed programmatically, not just visually inspected.

**Consequence not asked for but incidentally fixed**: the public map's PRU/nursery mis-colouring gap (flagged in `schools-in-bounds/route.ts`'s own comment as "worth a decision, not an assumption") is now resolved for `sector`/`phase` *tagging* — a PRU/nursery school's dot will read as untagged (grey, no sector colour) rather than falsely "State"/"Junior" — as a side effect of fixing `sectorTag()`/`phaseTags()` at the source. **Not** fixed: the bounding-box SQL query itself still fetches these rows (no row-level exclusion added to that route's query) — deliberately out of scope for this brief, which is about the Data View's own matching pipeline, not a mandate to re-architect the public map's row-level filtering. Flagged here so it isn't confused with a completed fix to that route.

### 4. Boarding-ratio anomaly (Downe House 104%, Benenden 111%)

**Finding: did not reproduce against live data.** Investigated directly: fetched real current-period (2025) DfE census facts for both named schools via `lookupReferenceData` (the same source `roll-data.ts`'s `finishSnapshot()` reads). Downe House: `boarders_total=459`, real age-sum roll=535 → ratio 85.8%. Benenden: `boarders_total=424`, real age-sum roll=480 → ratio 88.3%. Neither exceeds 100% at any of the four most recent periods checked (2022-2025). Then re-scanned the **entire real §6.1 candidate pool** (`establishment_type_group='Independent schools'`, `boarders_name` not null/not "No boarders" — confirmed **403** real schools, exactly matching the brief's own quoted "≈403") at the current 2025 period: **zero** schools anywhere in that pool have a ratio exceeding 100% today.

This strongly suggests the brief's cited anomaly reflects a **stale/since-corrected ingest snapshot** at the time the design conversation happened, not a live, currently-reproducible bug — the underlying DfE census data has evidently been re-synced since. Per the brief's own explicit fallback instruction, the interim safeguard was applied anyway (cheap, harmless, protects against a future data-refresh regression): a new `boardingRatio()` helper in `typology.ts` clamps any ratio to 100% and logs a `console.warn` flag for manual review if the raw ratio ever exceeds it. `boardingTag()` and the new §6.1 boarding-quintile list logic (§6 below) both go through this one shared helper, not two separate caps.

---

## §6.2 — VicData Sets schema decision

**Decision**: VicData-recipe default comparator lists (§6's List 1/2/3) are **not** persisted as `saved_sets` rows this round — no schema migration. They're computed live, per viewed school, by a new `src/lib/default-comparator-lists.ts` module, and presented in the Data View's sidebar set-picker as selectable options identified by a synthetic key (e.g. `recipe:nearest_10`, `recipe:in_la`, `recipe:boarding_quintile`) rather than a `saved_sets.id`. A member's own self-curated sets (existing `saved_sets` rows, unchanged schema, fetched exactly as `/sets/page.tsx` already does) appear in the same picker alongside them, distinguished by an `id: string` (real UUID) vs. a `recipe: string` (synthetic key) discriminated union in the picker's own type — no backend concept of "a set with no owning school" is needed to make that UI work.

**Why not the schema change the brief describes** (a nullable `school_account_id` plus a recipe-identifying column): that's a real, irreversible-ish schema decision — exactly the kind of thing brief §0.4 flags as worth not guessing at if a materially simpler, fully-reversible alternative exists. It does here: nothing about "a recipe list, computed on demand" *requires* a database row until a member actually wants to **save** one as their own editable/shared set — at which point that's just an ordinary `saved_sets` insert (owner_membership_id set normally, `school_account_id` filled from the viewing member's own account, `config.generated_from_recipe` recording which recipe seeded it for provenance) using the **existing** schema unchanged, not a new nullable-FK path. If Guy wants "VicData recipes" to become real, independently-manageable rows later (e.g. an admin UI to add/edit recipes without a code deploy), that's a genuine, separate schema project — this decision doesn't foreclose it, it just doesn't build it speculatively now.

**What would need to change if Guy decides differently**: add the nullable `school_account_id` + recipe-identity columns to `saved_sets` (or a new `vicdata_recipe_sets` table) as its own migration, and change the Data View's set-picker to fetch recipe rows from the DB instead of computing them from `default-comparator-lists.ts` directly. The computation logic itself (which schools end up in "nearest 10," etc.) doesn't change either way — only where its *identity/label* lives.

---

## Findings and decisions from the build itself

### CRITICAL FINDING — the site-wide access gate silently broke every existing bearer-token API route, not just this build's own

`src/proxy.ts` (this app's launch-gating Basic Auth middleware, per the full build brief) and every paid API route's own auth (`/api/paid-trends`, `/api/comparator-set-peers`, the new `/api/data-view/*` routes) both read the single `Authorization` HTTP header for two genuinely different purposes — the gate wants `Basic <user:pass>`, the routes want `Bearer <supabase-jwt>`. A browser that has already answered the gate's Basic Auth prompt caches those credentials and auto-attaches them to ordinary page loads, but a `fetch()`/XHR call that sets its **own** `Authorization: Bearer ...` header (exactly what every one of these routes' client code does) **replaces** the browser's cached header for that one request — there is only one `Authorization` header per request. Confirmed live against the running dev server, not guessed: `curl` with only a Bearer token against `/api/data-view/default-lists` returned the gate's own 401 "Authentication required"; the **already-shipped** `/api/paid-trends` route reproduces the identical failure. With `ACCESS_GATE_ENABLED=true` (the current `.env` default, and the stated "Active build and private testing" launch phase every real session up to now has been in), **every bearer-token-gated feature in this entire app has been unreachable from a real browser** — not a regression introduced this session, a pre-existing, apparently never-noticed bug in the gate itself.

**Fix applied**: `proxy.ts` now treats a syntactically-Bearer `Authorization` header as satisfying the site-wide gate on its own (`isBearerRequest` check, added before the existing Basic Auth check). This does not weaken the gate's real purpose (keeping an anonymous stranger off the site and off search engines, per the launch-gating doc) — every route downstream still independently verifies the token against real Supabase auth/RLS before returning anything; a garbage or missing bearer value still gets rejected by that real check, just as before this fix. Verified live: the exact same Bearer-only request that returned 401 before the fix now returns 200 with real Acland Burghley data; a request with no auth at all still gets the gate's 401.

**What would need to change if Guy decides differently**: this is a security-adjacent judgement call made under the brief's own "don't stall the whole session" instruction, since leaving it unfixed meant the entire Data View (and every other paid feature) was non-functional in the browser under the current, default gate configuration. If a different fix is preferred (e.g., moving the app's own bearer token out of the `Authorization` header into a custom header, so the gate's header stays exclusively for Basic Auth), that's a real, slightly larger refactor touching every existing paid API route's client call sites, not just this build's new ones — flagged here rather than attempted speculatively.

### §4 — Shell and shared filter state

- **Route**: `/schools/[urn]/data`, a new client-component route (matching this repo's existing `/account`, `/sets`, `/sets/[id]` pattern — there is no server-side session/middleware to gate on instead, confirmed by reading `src/proxy.ts` directly: it's Basic-Auth-only, unrelated to member identity). Deliberately not a tab *within* the existing public `/schools/[urn]` page — that page is free/paid-content-woven-together by design; this is a wholly separate, paid-only surface with its own auth gate.
- **Topic tabs**: the brief's "chevron submenus where the public page pattern already has them" doesn't match this repo's current public page (it uses inline `ComingSoonCard` placeholders, not a tab strip at all — confirmed by reading the file directly). Built a plain topic-tab row from scratch matching the brief's *described shape*, not a pattern that doesn't actually exist yet to copy.
- **Boarding-status as a shared FILTER** (not a set-membership filter): given the existing `docs/OPEN_QUESTIONS.md` entry ("boarding excluded from surrounding-schools matching... deliberately deferred to subscription-tier percentage-based filtering") explicitly anticipates this exact feature, and given the brief groups it with phase/gender as filters that "recompute every school's value" (never narrowing the tick-list itself, which stays the sole comparison mechanism), boarding-status here means: which real number to show (boarders vs day-pupils headcount), not which schools qualify. Options are the two real DfE census figures (`Boarders`/`Day pupils`), not typology.ts's 3-way `BoardingTag` display category — see `data-view-filters.ts`'s own header comment for the full reasoning, including the real data constraint that boarding figures have no per-phase/age breakdown at all, so boarding+phase can't be honestly combined (boarding wins outright when both are active, with a caption saying so).
- **"Early Years" phase-filter band**: not a `typology.ts` `PhaseTag` (no such tag exists) — implemented as a real, separate age-slice (ages below `EARLY_YEARS_PROXY_AGE_THRESHOLD`), selectable alongside Junior/Prep/Senior/Post 16, since the brief names it explicitly as one of four bands. "16+" in the brief's prose maps directly to the existing "Post 16" tag — kept as "Post 16" so the filter stays in typology.ts's one real vocabulary rather than inventing a second name for the same thing.
- **Tick-list default/overflow**: no explicit number given in the brief for how many of a set are ticked by default vs. collapsed into the "more in this set" overflow — used 10 (matches List 1's own "nearest 10" scale, the most common case), the first N schools of whichever set is active tick by default, the rest sit in a collapsed, unticked, expandable section.
- **PDF export button placement**: shown for all three views (brief §9: "applies to Map, Dashboard, and Rankings") in one shared shell-level location rather than duplicated per view.

### §5 — Dashboard

- **Filtered trend badges**: a school's phase mix can genuinely differ between 2019 and now (a school that added a sixth form, say), so a "since 2019" trend badge under an active phase/gender filter recomputes the 2019 figure through the *same* slice, not just the current one — required threading a second, 2019-anchored age/gender breakdown (`ageGenderCounts2019`) through `DataViewSchoolProfile`, not just the current-period one the brief's own wording focuses on.
- **Known, logged simplification**: the boarding+gender filter combination specifically at the 2019 anchor falls back to the whole-boarding-population figure rather than a gendered slice (boarders_male/boarders_female aren't separately tracked at the historical anchor in this build, only at the current period) — every other filter combination, and the current-period boarding+gender combination, is a real, exact slice, not an estimate.
- **Roll trend chart**: plots whole-school roll across all real historical periods regardless of the active phase filter (per-period age-banded breakdowns beyond the current/2019 anchors aren't threaded through the profile) — every OTHER Dashboard card does respect the active filter; this is the one deliberately scoped exception, called out in the chart's own caption when a phase filter is active, not silently inconsistent.
- **Through-school size-band label**: bands are simple tertiles over the ticked group's own range (this build's own chosen-not-discovered default, same discipline as several existing provisional thresholds in this codebase, e.g. `narrative-config.ts`'s `TOPIC3_SIZE_BAND_THRESHOLD`) — no other guidance given in the brief.

### §6 — Default comparator lists

- **List 2's school count**: the brief gives an explicit count for List 1 ("nearest 10") but none for List 2 ("in Local Authority") — capped at 30 as a reasonable tick-list size (verified live: Woldingham's real Surrey in-LA pool naturally produced exactly 30 after the real roll-data skip-and-backfill, Acland Burghley's real Camden pool produced 19 without hitting the cap) — a real, load-bearing number, not an arbitrary label.
- **List 3 performance, measured**: the Charterhouse-recipe boarding-quintile scan across the real ≈264-403-candidate national pool took **71.6 seconds** end to end with the batched-fetch-in-chunks-of-40 approach exactly as specified. Parallelised the chunk fetches (bounded concurrency of 4, still respecting the proven-safe 40-per-request chunk size) which cut this to **~41 seconds, still too slow for an interactive click**. Rather than force a precomputed table this round (explicitly deferred by the brief itself, §10), split List 3 into its own **lazy** endpoint (`/api/data-view/boarding-quintile-list`) — the sidebar's initial load (List 1 + List 2, both consistently under 3 seconds even for a Woldingham-shaped school) never blocks on it; a member sees a "computing…" state only if they actually select that option. Logged here as the real, measured evidence for the brief's own "if batching alone genuinely can't keep this responsive... log that finding" instruction.
- **State boarding List 3 — the brief's own "not yet sized" question, now answered**: a real, live count (`establishment_type_group` in the three mainstream state groups, `boarders_name` not null/not "No boarders", real non-zero current `boarders_total`) found **60** real schools nationally (Old Swinford Hospital, Wymondham College, Duke of York's Royal Military School, Ripon Grammar School among them) — workable to quintile (12 schools/quintile), not "too small" in the sense the brief worried about. Reuses the exact same headcount-basis recipe as Independent Senior boarding rather than a special-cased mechanism. Flagged honestly: this population is real but far sparser geographically than the ~403-school independent pool the 30km bottom-quintile cap was tuned against, so a state-boarding school in a lower quintile may see a thin (even empty) List 3 group — an honest reflection of a genuinely thin population, not a bug, consistent with this codebase's existing "honest degrade" precedent (`findSurroundingSchools`' own skip-and-backfill).
- **Special Schools sector**: absent from the brief's own §6 table entirely. Falls back to the same shape as "State" (List 1 + List 2, no List 3) since `nearest_schools`' RPC already has real special-school-aware matching (`target.is_special`, a pre-existing fix from 2026-09-30) that `findSurroundingSchools` benefits from unmodified — the safest, most consistent default absent explicit guidance, not a new mechanism invented for this.
- **Relaxed gender rule**: implemented as a new opt-in `genderMode` parameter on `findSurroundingSchools()` (default `"exact"`, so the existing free-tier public surrounding-schools stat/list is completely unaffected), rather than changing that function's existing behaviour — the brief's own "reuse findSurroundingSchools()... plus the relaxed gender rule" reads as exactly this: reuse the pipeline, extend the one differing rule.
- **FE college List 1/2**: reused/extended `surrounding-fe-colleges.ts` (the existing FE-to-FE distance mechanism, deliberately never the mainstream `nearest_schools` RPC — that module's own header comment already explains why an FE target passed through the mainstream RPC matches nothing sensible). List 2 ("all 16+ provision in the borough") counts a school as real 16+ provision via three real signals: FE sector, a standalone "Post 16" phase tag, or a Senior/through school with a genuine stated leaving age of 17-19 — membership-only, not gated on real roll data existing (a real "does this exist" question, not a sizing one).

### §8 — Map

- **Trend colour scale**: a genuinely new, provisional palette (`trend-colours.ts`) — not yet run through the colourblind-safety validator the historical trend charts were checked against (this codebase's own standing precedent, per the chart palette doc). Built from a real, named diverging ColorBrewer-style ramp (ends more likely to already be reasonably colourblind-tolerant than a hand-picked pair) as a defensible starting point, explicitly flagged here as provisional, same discipline as the gender-colour pairing already logged as provisional elsewhere in this codebase.
- **Map architecture**: a new component (`MapView.tsx`), not a variant of the existing `SchoolMap.tsx` — that component's whole design is a live, moveend-triggered viewport query against a national candidate pool; this map renders a fixed, already-fetched comparator set, a genuinely different problem shape. Shares what's actually shared: BNG conversion (extracted to `src/lib/bng.ts` so the two don't each keep their own copy), `TAG_COLOURS` for Sector mode, `MapBoxCollapseToggle` for the floating-box chrome.
- **Unticked members on the map**: shown faded/hollow rather than hidden entirely, clickable to add back into the ticked comparison — lets a member browse the whole set geographically without losing track of who's currently being compared, while keeping the tick-list itself as the one real comparison mechanism (clicking a faded dot ticks it, it doesn't silently start comparing against something never explicitly chosen).

### §9 — PDF export

- **No existing pattern found in this repo**: grepped for `window.print`/`@media print`/`afterprint` — zero hits anywhere. VicDash (the cited reference) is a separate Phase 2 repo not present here, so its exact implementation and branding couldn't be inspected directly, only the brief's own description of it. Built fresh matching that described shape (`window.print()` + a `@media print` stylesheet, no PDF-generation dependency) since it's the best available reference, not a verified port — flagged so this isn't mistaken for a confirmed-identical copy later.
- **Branding**: moot for this reason — there is no existing exporter output to check for stray VicDash/Victoria Consultancy branding. The Data View's print output only ever renders this app's own existing UI (which is already plain "VicData" branding throughout, per `NavBar.tsx`), so there was nothing to separate out.
- **No afterprint-timing bug to carry forward**: the lesson is real (documented in the brief) but doesn't apply here as built — this export does no DOM/palette mutation of its own at all (no "enter print mode, then revert" step), so there's nothing for `afterprint` to clean up. Logged explicitly in `PdfExportButton.tsx`'s own comment as a trap for whoever adds a print-time state mutation later.

### Housekeeping (brief §0, carried out as part of this session)

- Confirmed `saved_sets.school_account_id` genuinely `not null` (§6.2 above) by reading the real migration file, not assuming.
- Confirmed the referenced source docs `vicdata_phase3_member_data_view_spec_v1.md`, `vicdata_membership_area_ideas_v1.md`, `vicdata_ingest_schema_and_build_plan_v2.md`, `roll pipileine geolocation.md`, and `vicdata_shape_panel_wording_sharpening_v1.md` (brief §11) **do not exist anywhere in this repo** — the brief's own framing ("consolidates... into one build-ready document... this brief wins") already anticipates this; noted here only so it isn't mistaken for an oversight later.
- Pre-existing lint debt found, not fixed (genuinely out of scope — unrelated to anything this build touched): `src/app/account/page.tsx` (×2) and `src/components/SchoolSearch.tsx` (×3) already fail `react-hooks/set-state-in-effect`/`react-hooks/refs` on the base commit, before this session's changes (confirmed via `git stash`). `src/components/PeerTrendChart.tsx` and `src/components/SchoolMap.tsx` each have one pre-existing unused-variable warning, same confirmation. This session's own new code lints and typechecks clean throughout.

---

## Live incident (2026-09-05): "This page couldn't load" on `https://vicdata.co.uk/schools/100053/data`

**Reported**: Safari failed to load the Data View for a genuinely logged-in, genuinely approved member, with reload "flashing to another screen before landing back on the same error."

**Confirmed live, directly against production** (not assumed):
- The `proxy.ts` access-gate fix from the previous round **is deployed and working**: a request to `/api/data-view/default-lists` carrying only a Bearer token (no Basic Auth) now reaches the route itself (403 from the route's own membership check for a garbage token; 200 with real data for a genuine one) rather than the gate's 401 — verified with both a garbage token and a freshly-generated real session for the one real test membership in this database (`guybeckett@mac.com` / Acland Burghley, URN 100053).
- The page itself returns a clean `200` with no server-side redirect at any point (checked with `curl -v`, both anonymous and Basic-Auth-authenticated) — whatever is failing is not an HTTP-level redirect loop or a server error.
- No `router.push`/`router.replace`/`redirect()` exists anywhere in `DataViewShell.tsx` or its render path except one **user-click-triggered** `router.push` inside `MapView.tsx`'s marker handler — ruled out as an automatic loop source by direct code inspection.

**Could not reproduce directly**: no browser tool was available in this session (`claude-in-chrome` reported "extension is not connected" both times it was tried), so the actual client-side JavaScript failure could not be observed or captured from a real Safari session. The root cause below is the most plausible explanation found by code audit, not a confirmed-and-reproduced diagnosis — flagging this honestly rather than claiming certainty.

**Two real bugs found and fixed in the audit, either of which could plausibly produce exactly this symptom**:
1. `DataViewShell.tsx`'s Step 1 effect (the client-side membership check) had no `try/catch` and silently discarded the membership query's own `error` field — the *exact* class of mistake `account.tsx`'s and `sets.tsx`'s own pre-existing comments already warn about from a prior incident ("silently rendering... because the error... used to be discarded"). If `getSession()` or the membership query ever throws or returns a real error (a transient network blip, a cold-start timeout against the remote Supabase project — plausible on Render's free/starter tier), the effect's promise rejected with nothing catching it, and `loadState` stayed stuck at `"checking"` **forever** — an indefinite "Loading…" with no escape hatch. A user reloading would re-run the same effect and could hit the same transient failure again, which is at least consistent with "flashes to Loading… before landing back on the same stuck state." Fixed: every path through both loading effects now reaches a definite terminal state, real errors are logged to the console and surfaced as a real "error" state with a Reload button.
2. Added a 12-second client-side timeout as defence in depth regardless of the fix above: if the app is ever still on "checking"/"loading" after 12s for any reason (including a genuinely hung request, not just a rejected promise), the user now gets a visible "taking longer than expected" message with a Reload link and a link to the new `/member` fallback, rather than an unbounded spinner.

**A separate, real bug found while auditing (not the reported symptom, but worth fixing regardless)**: `DashboardView.tsx`, `RankingsView.tsx` and `MapView.tsx` were importing `profileToFilterableData`/`profileToFilterableData2019` as real value imports from `data-view-profiles.ts` — a **server-only** module (it imports `createServerAnonSupabaseClient`, per that file's own "never import into client components" docstring). This doesn't crash (nothing in that module runs at import time), but it violates this repo's own established server/client boundary and pulls needless server-only code into the client bundle. Fixed by moving both pure functions to `data-view-serialize.ts`, the file already designed to be safe on both sides.

**What would need to change if this recurs after the fix**: get real browser DevTools/console access against the live failure (this session had none) — that would show the actual thrown error directly rather than requiring inference from code audit alone.

## Members' home page (added 2026-09-05, per direct request)

New `/member` route (renamed from an initial `/home`, per Guy's own correction -- `/home` reads as the PUBLIC home page's name, and `/` already serves that unchanged): checks login, looks up the user's own approved memberships (same qualified-embed query `account.tsx` already uses), and for the common single-membership case does a client-side `router.replace` straight to `/schools/{urn}/data`. Handles the two other real cases honestly rather than assuming the common one: zero memberships (points to `/join`), more than one (a plain chooser list — no attempt at picking a "default" school this round). `login/page.tsx` now sends here instead of `/account` post-login; `NavBar.tsx` gained a "Home" link for logged-in users (there was previously no way back to it once you'd navigated away). Verified the underlying query directly against the one real test membership in this database (Acland Burghley) — resolves correctly to a single-membership redirect target.

**Logout button** (added 2026-09-05, per direct request): there was no way to log out anywhere in the app. Added to `/account` for now (the one page every logged-in member can reach via `NavBar`'s own "Account" link) — a plain `supabase.auth.signOut()` followed by a redirect to `/`. Not placed in `NavBar` itself this round since that would need a small design decision (icon-only vs. text, dropdown vs. inline) the request didn't ask for — flagged as a natural next step if a persistent, always-visible logout is wanted.

---

## Live incident #2 (2026-09-05): Map view stuck on "Loading school data…" forever, Acland Burghley's real Nearest-10 set

**Root cause found and reproduced directly this time** (not inferred from the symptom, per the request that prompted this): `data-view-serialize.ts`'s `serializeProfile()`/`deserializeProfile()` were written before `ageGenderCounts2019` existed on `DataViewSchoolProfile` (it was added later, for filtered "since 2019" trend badges — see that field's own comment) and never updated to also convert it. A `Map` that goes through `JSON.stringify`/`NextResponse.json` with no explicit conversion silently becomes `{}` — no error, no warning, just an empty object masquerading as the real type. TypeScript couldn't catch this: both functions were fully type-correct: the bug is a **runtime shape** mismatch a JSON round-trip introduces, not something the type system tracks.

**Reproduced end to end, exactly as reported**, before writing any fix:
- Started a local dev server, generated a real session for the one real test membership in this database, and called the *exact* URL the client calls (`/api/data-view/schools?anchorUrn=100053&urns=...`) with the *exact* real 10 URNs from Acland Burghley's own real Nearest-10 set. The API itself: **1.55 seconds**, 200, real data — confirming the report's own instinct that this should be (and is) the fast path, not the slow List 3 recipe.
- Inspected the raw response directly: `ageGenderCounts2019` was `{}` on every profile, not the real per-age array `ageGenderCounts` (right next to it) correctly was.
- Ran the client's own `deserializeProfile` → `profileToFilterableData2019` → `filteredCount` chain against that real response in isolation: threw `counts is not iterable` immediately — the exact TypeError `for (const [age,c] of counts)` throws when `counts` is a plain object, not a Map.
- This throw happens inside `MapView.tsx`'s marker-drawing effect (Map is the default landing view, so it hits this on first load for essentially any real school with real 2019 history) — a synchronous throw partway through the marker loop that never got caught anywhere, leaving the map half-built and the surrounding "Loading school data…" state (already `false` on the actual fetch, which succeeded) with nothing left to replace it. `DashboardView.tsx`/`RankingsView.tsx` call the exact same function and would have hit the identical crash had the user reached them first.

**Fixed at the actual source**: `serializeProfile`/`deserializeProfile` now convert `ageGenderCounts2019` the same way `ageGenderCounts` always was. **Re-ran the exact same reproduction** against the fixed code — the API response now carries a real array, and the full deserialize→compute chain succeeds for all 10 real schools with real, sensible numbers (e.g. Acland Burghley 1,252 now vs. 1,072 in 2019).

**Defence in depth added on top of the source fix**, since this bug specifically demonstrated that a type-correct-looking value can still be the wrong runtime shape after a JSON round-trip:
- `data-view-filters.ts`'s `sumAgeGender()` now guards with `counts instanceof Map` before iterating — degrades to "no data for this slice" instead of throwing if a similar mismatch ever recurs for any field.
- `MapView.tsx`'s per-marker drawing is now wrapped in its own `try/catch` — one school's bad data logs a console error and skips that one marker, it no longer aborts the whole map mid-loop.
- `DataViewShell.tsx`'s Step 3 profile-fetch effect gained the same try/catch + real error state the Step 1/2 fix already added (it had the identical missing-error-handling gap, independent of the serialization bug above — a `fetch()`/`res.json()` failure here would previously also have hung `profilesLoading` forever with no way out).
- A real React **Error Boundary** (`DataViewErrorBoundary.tsx`) now wraps whichever of Map/Dashboard/Rankings is active, remounted (clearing any caught error) on every view switch. This is new: a render-phase throw in any view previously had no boundary at all, meaning React's default behaviour (unmount the whole tree) would have taken down the entire Data View, not just the one broken view. Neither of this incident's two real bugs happened to be a render-phase throw specifically (both were effect-phase), so this boundary wasn't what caught THIS bug — but it's exactly the kind of gap this incident surfaced, and there was nothing stopping the next one from being a render-phase error instead.

**No test suite exists in this repo** (flagged as a real gap in an earlier housekeeping entry above) that would have caught this before it shipped — a fixture test asserting a round-trip through `serializeProfile`/`deserializeProfile` preserves every field's real shape would be the natural regression test if a suite existed. Logged here rather than added speculatively, matching this repo's own existing "no tests yet, worth it, not done this round" posture elsewhere.

---

## Live incident #3 (2026-09-05): two layout regressions — no full-bleed, filter bar position shifts between views

**Root cause #1 (full-bleed)**: `DataViewShell.tsx`'s own top-level wrapper was `mx-auto max-w-6xl px-4 sm:px-6` — a centred, 1152px-capped container, producing large left/right margins on any wider screen. Confirmed directly against the wireframe's own real source (not a screenshot guess): the published canvas artifact embeds each board's actual HTML/CSS as a JSON blob inside the artifact's own saved content — extracted and read it directly (`.shell{width:100%;height:100%}`, no max-width anywhere in any of the three boards). Removed the capped wrapper entirely; the shell now fills the viewport edge to edge, matching the wireframe.

**Root cause #2 (filter bar position)**: the phase/gender/boarding filter bar used to be rendered in two structurally different ways depending on the active view — an in-flow block above the view content for Dashboard/Rankings, but a small floating overlay box (`absolute left-3 top-3 w-64`) pinned to the map canvas's own top-left corner for Map. Two different DOM positions, so of course the visible position shifted on every view switch.

**A genuine wrinkle found while fixing this**: the wireframe *itself* isn't internally consistent here. Read directly from the source (not assumed): Map's own `.map-filter-bar` is `position:absolute` and scoped to `.main`'s width only (floating over the map, starting to the right of the sidebar); Dashboard's and Rankings' `.filter-bar` instead sits ABOVE the sidebar+main split entirely, spanning the FULL PAGE width (over where the sidebar sits too), with the view-switcher in its own separate `.main-subheader` row scoped to `.main`. Two of the three boards agree with each other and not with the third. Resolved in favour of the Dashboard/Rankings treatment — it's the majority position among the three boards, and it's what the bug report's own wording ("full width... same screen coordinates") actually describes. Rendered ONCE, unconditionally, in `DataViewShell.tsx` directly under the topic tabs and above the sidebar+main row, for all three views — there is structurally only one instance of it in the DOM now, so its position cannot drift between views again. `MapView.tsx` no longer renders a competing copy; it only receives `filters` as a plain prop. The view-switcher moved to its own row at the top of the main panel (matching Dashboard/Rankings' `.main-subheader`), applied consistently to Map too.

**Trade-off, logged deliberately**: the brief's original §8 instruction ("Map's filter bar is a floating, collapsible overlay... consolidated with the view switcher") is no longer literally true — the filter bar is in-flow for all three views now, not floating over the map pixels, and the view-switcher sits in its own row rather than combined into the filter bar. The underlying INTENT (don't let the filter bar permanently eat into the map's vertical space) is preserved via a single shared collapse toggle (`filterBarCollapsed` in `DataViewShell.tsx`, replacing the two separate collapse mechanisms `MapView.tsx` and the old per-view `FilterBar` placement each had) — collapsing it shrinks the bar to a one-line "Show filters" link, for all three views identically, verified live (screenshot below). Chose exact positional consistency over literal overlay behaviour because the two are structurally incompatible without fragile pixel-matching between an absolutely-positioned box and an in-flow row, and positional consistency was the explicit, more specific, more recent instruction.

**Verified visually, not just by code review** — no `claude-in-chrome` browser connection was available in this session (checked twice), so used Playwright directly (already installed locally, `node_modules/.bin/playwright` + a cached Chromium build) to drive a real browser against the local dev server: generated a genuine Supabase session for the one real test membership in this database, injected it into `localStorage` in supabase-js v2's real storage format, navigated to `/schools/100053/data` with real Basic Auth credentials, and screenshotted all three views plus the collapsed-filter state. Confirmed directly from the screenshots: sidebar flush left, main panel to the right edge, filter bar at the identical y-coordinate/width/content across Map → Dashboard → Rankings, real data throughout (Acland Burghley: 1,252 roll/+17% since 2019/Pyramid→Top-step, real Nearest-10 list, real map markers, real rankings), and the collapse toggle correctly shrinking the bar and returning the reclaimed height to the map. Screenshots were local files, not committed (`/tmp/vd_*.png`, this session's Playwright output) — worth re-capturing at review time rather than treating these as a permanent artifact.

---

## Testing-only addition (2026-09-05): switch your own account's school, from `/account`

**Schema confirmed, not assumed** (per direct request) — read `supabase/migrations/20260807092000_school_accounts_and_memberships.sql` directly rather than trusting the request's own working assumption. There is **no single field on a profile/member record** naming "which school." The real link is a join: `school_memberships.profile_id` → `school_memberships.school_account_id` → `school_accounts.school_urn`, gated by `school_memberships.status = 'approved'`. Access to the Data View (and everywhere else membership-gated) is enforced via RLS reading through that join, not a direct foreign key on the profile itself.

**Built**: `/api/testing/switch-school` (server route, service-role) + a picker section on `/account`, reusing `SchoolSearch` — the same component the public home page (`src/app/page.tsx`) and the Data View's own "search to add" already use, per the request's own "no new UI pattern" instruction. Selecting a school: finds-or-creates a `school_accounts` row for it, removes every existing `school_memberships` row for the caller's own profile, inserts one new approved+admin+account-holder membership there, and redirects to `/schools/{urn}/data`. "Switch," not "add" — deliberately replaces rather than accumulating, per the request's own "without a separate real membership per school" framing; logged as the one genuinely ambiguous reading in that request, though the most direct one.

**Gating decision** (the request offered a choice, logged since it's genuinely ambiguous): restricted behind an env-flag pair (`NEXT_PUBLIC_ENABLE_TESTING_SCHOOL_SWITCHER` gates whether `/account` even renders the picker; `ENABLE_TESTING_SCHOOL_SWITCHER` is checked again server-side by the route itself, independently) rather than a hardcoded personal email. Reasoning: hardcoding a specific email into committed source both bakes a personal identifier into a repo and is more brittle (breaks silently if that email ever changes); an env flag is unset/false in every real environment by construction, is visually obvious as a testing toggle by its own name, and still can't be turned into a real privilege-escalation path even if somehow visible to someone else, because the route only ever reads/writes the CALLING user's own `profile_id` (resolved from their own verified bearer token, never client-supplied) — there is no path from "the flag is on" to "I can modify someone else's membership." Both flags are set in the local `.env` only; enabling this on the live Render deployment (if ever wanted for testing directly against `vicdata.co.uk`) is a deliberate, separate step for whoever manages that environment, not done as part of this change.

**Two real bugs found and fixed via live end-to-end testing, not just code review** — deleting a `school_memberships` row can be blocked by FOUR separate foreign keys across the schema (found by grepping every migration for `references public.school_memberships`, not guessing): `school_accounts.account_holder_membership_id` and `.pending_account_holder_membership_id` (neither has an `ON DELETE` action, so Postgres defaults to `RESTRICT`), `school_memberships.approved_by` (self-referencing), and `saved_sets.owner_membership_id`. The first version of this route only cleared the two `school_accounts` columns and didn't check the delete call's own result — it silently failed the moment a real dependent row existed (a `saved_sets` row this very session had created while testing the Data View earlier), leaving the OLD membership alive alongside the new one instead of "switching." Caught directly: ran the actual flow with Playwright, screenshotted `/account` afterward, saw two schools where there should have been one, then reproduced the exact Postgres error via a direct script call rather than guessing. Fixed by clearing/removing all four dependent references before the delete, and by actually checking the delete's error. A second, related bug in the same fix: the account-holder handoff for the new membership read `account.account_holder_membership_id` from BEFORE the cleanup step, so it could act on a stale value — changed to unconditionally set the new membership as holder, which is also just the more correct behaviour for a tool whose whole job is leaving a clean, unambiguous state after every switch.

**Verified end to end, three consecutive switches, real schools, real screenshots**: Acland Burghley (state day) → Charterhouse (independent boarding) → Yerbury Primary (state primary) → City Lit (FE college) — `/account` correctly showed exactly one membership after each switch, each redirect landed on the right real URN's `/schools/{urn}/data`, and a final direct database check confirmed exactly one `school_memberships` row for the test profile and zero orphaned `saved_sets` rows.

---

## Map view layout v1 (2026-09-05): full-canvas overlay redesign

Per direct request, explicitly a first pass to be iterated on rather than a final design: the Map view's own internal layout changed from in-flow controls (view switcher + export button in a shared subheader row, colour-by/size folded into one sidebar-style box) to a full-bleed canvas with every secondary control as a floating overlay on top of it. Scoped to Map only — Dashboard, Rankings, and the shared filter bar are unchanged.

**Two judgement calls, not literally specified in the request:**
- **Sector colour-key position/width.** The request specifies the *trend* colour key's width must match Leaflet's own zoom control (confirmed 26px from `leaflet.css`'s `.leaflet-bar a` rule, not guessed) and sit directly above it. It doesn't say anything about the *sector* mode's swatch-list legend, which occupies the same "colour meaning" slot when `colourMode === "sector"`. A 26px-wide column can't fit sector name text, so sector mode keeps a normal-width box in the same general right-side position instead of matching the zoom control's width. Worth confirming once seen live — it may read as inconsistent given the trend key's strict width match right next to it.
- **Dot-caption position.** The request explicitly left this to judgement ("bottom of the map seems reasonable"). Placed bottom-centre, clear of the size legend (bottom-left) and trend/sector key (right side, above zoom control).

**A real, pre-existing gap this surfaced**: `DataViewShell.tsx`'s sidebar+main row already used `flex-1`/`min-h-0` (from the Live incident #3 full-bleed fix), clearly anticipating a "stretch to fill the container's height" layout — but nothing above it in the tree ever marked THIS route's own root as the flex item that should grow into the leftover space. `body` (`src/app/layout.tsx`) is already a flex column with a real baseline height (`min-h-full`, resolving against `html`'s `h-full` = the viewport) — the standard minimal "sticky footer" recipe — but `DataViewShell`'s root was rendered as a plain block, not a `flex-1` item of that column, so it never grew to fill the leftover space. Invisible before because every view's content was naturally taller than one viewport anyway. Fixed by marking `DataViewShell`'s own root `flex-1 min-h-0` instead — a no-op for tall views (Dashboard/Rankings: no spare space to distribute either way) but gives Map real height to fill now its controls are floated off the in-flow layout. (First attempt used `min-h-[100dvh]` directly on the root instead — wrong, since that STACKS on top of the navbar/tabs/filter-bar height rather than sharing the one real viewport height, pushing real content below the fold. Caught by checking `document.body.scrollHeight` against `window.innerHeight` with Playwright rather than trusting the screenshot alone.)

**A second real bug found via the same live-reproduction discipline**: below the sidebar's `lg` breakpoint (sidebar stacks above the map), a long comparator list can leave the map's `flex-1` share of the remaining height at ~0 — the map's own content wrapper now has `min-h-[480px] sm:min-h-[560px]` as a floor under `flex-1`, matching the fixed heights this replaced but as a minimum rather than the only size.

**A third real bug, Leaflet-specific**: even with real container height, Leaflet's map tiles rendered blank at a narrow (900px) viewport. Root cause, confirmed by reading actual computed styles via Playwright rather than guessing from the screenshot: `getComputedStyle` on the map's own root showed `height: 0px` despite its immediate parent measuring a real 560px — a percentage-height child (`h-full`) doesn't reliably resolve against a containing block whose box height comes from `min-height` rather than a literal `height` (a genuine, observed Chromium behaviour). Fixed by making the map's root `absolute inset-0` instead of `relative h-full w-full`, sidestepping that resolution rule the same way its own children already do. Also added a `ResizeObserver` calling `map.invalidateSize()` on the map container, since Leaflet measures its container once at creation and won't notice if that size settles later under a flex layout — belt-and-braces alongside the sizing fix, not a substitute for it.

**Verified live** with Playwright against local dev, real session, real data (Acland Burghley, Nearest-10 any-LA list): all three views screenshotted at 1440×900 (Map's overlays all present and correctly positioned, Dashboard/Rankings pixel-identical to before, filter bar position unchanged across all three), Map re-screenshotted after switching to Sector colour mode, and Map re-screenshotted at a 900×700 narrow viewport (below the `lg` sidebar-stacking breakpoint) to confirm the map still renders with real tiles and all overlays after the three fixes above.

**Noticed, not fixed (out of scope for this task)**: Sector colour mode's marker fill colours render as flat grey rather than each sector's real tag colour — traced to `tagColour()` reading `getComputedStyle` on the wrong element (the map's own root, an ancestor of `.vd-dataview-map`, which is where the CSS custom properties are actually defined — custom properties cascade to descendants, not out to ancestors). Looks pre-existing (predates this layout change), not introduced by it. Left alone since it's a colour-computation bug, not a layout one, and this task was scoped to layout only — flagging here rather than fixing silently or missing it silently.

---

## Map view layout, feedback round 1 (2026-09-05): trend colour key repositioned

Direct feedback on the v1 above: the trend colour key sat too close to the zoom control (it was a hand-tuned `bottom-[70px]` constant). Replaced with a real measurement: an effect reads the actual `getBoundingClientRect()` of the top-right control stack (export + colour-by) and Leaflet's own `.leaflet-control-zoom` element, computes the real vertical gap between them, and sizes/positions the bar to occupy 70% of that gap, centred within it — re-measured via `ResizeObserver` on both the map container and the control stack, so it stays correct across window resizes and map-height changes rather than only being right at the one size it was tuned against.

Dropped the "growing"/"declining" text captions above/below the bar in the same pass — they were extra vertical space outside the measured 70% box, and are redundant with the numeric tick labels (`+30%` down to `-30%`) plus the colour convention itself (blue = growing is already used elsewhere, e.g. the Dashboard's roll-trend line).

---

## UX refinements round 1 (2026-09-06): B1 -- FE college numbers were all zero

**Root cause, confirmed by reading the actual code path, not guessed**: `data-view-profiles.ts` already computed a real ILR-sourced fallback figure for FE colleges (`ilrFallbackRoll`) whenever real DfE census data was absent -- but that field was never consumed anywhere. Every view (Map/Dashboard/Rankings) computes what to show via `filteredCount(profileToFilterableData(profile), filters)`, which only ever reads `ageGenderCounts` -- genuinely empty for an FE college (no census coverage at all, a real and permanent gap, not a bug). So every FE college showed 0 everywhere, regardless of the real ILR data sitting unused right next to it.

**Fixed at the source**: replaced the old single flat `ilrFallbackRoll` (whichever of under-19/adult happened to be non-zero first, total only) with a real `feParticipation: { under19, adult }` structure, BOTH segments, each with a real male/female split (fetched via the same `dfe_fe_participation`/`dfe_fe_participation_adult`/`dfe_fe_participation_academy` sources, widened to request the `_male`/`_female` breakdowns alongside `_total`, reusing `ilr-participation-data.ts`'s existing per-entity parser rather than writing a second one). Wired this into `filteredCount` itself (`data-view-filters.ts`) as a new branch, so every consuming view gets a real number through the exact same function it already calls -- no per-view patching. "Post 16" doubles as the U19 label for an FE college (its existing real age range for a school, `[16, highAge]`, already matches "ages 16/17/18" for the overwhelmingly common `highAge = 18` case -- logged as an accepted simplification below); a new "Adult" band was added to `PhaseBandKey` for the 19+ figure, since schools have no analogue for it at all.

**Verified against real data** (not just types) before moving on: Ealing, Hammersmith and West London College (urn 130408) -- real U19 1,950 / Adult 9,690, Girls-only U19 820, all correct. City Lit (urn 130401) -- correctly 0 for the default/U19 case (it genuinely has no real under-19 participants), correctly 21,960 for Adult.

**Accepted simplification, logged rather than engineered around**: "U19 should resolve to ages 16/17/18" is implemented by reusing the *existing* Post-16 age range for a school (`[16, highAge]`), not a hardcoded `[16,18]`. For the overwhelming majority of real schools `highAge = 18`, so this is identical in practice; a school whose own statutory high age genuinely extends past 18 (no real instance found in this dataset while checking) would compare very slightly apples-to-not-quite-apples against an FE college's true under-19 ILR figure. Not worth adding compensating logic for a case with no known real instance, versus quietly narrowing what "Post 16" means for every other one of its existing uses.

**Genuinely out of scope this round, logged as a real follow-up**: FE colleges have no historical ILR trend wired in (only the latest real period is fetched, same as before this fix) -- `profileToFilterableData2019` honestly returns `feParticipation: null` for them, so an FE college shows no "since [year]" trend badge at all rather than a fabricated 0%. Porting FE colleges onto the same multi-year trend machinery schools get is real, separate work.

**The flagged, partly-corrupted note ("map shows la swap ? HE etc")**: the one unambiguous rule (exclude colleges with no real U19 students from lists meant to be U19/school-comparable) is applied to `findNearestFeColleges` ("Nearest 10 FE colleges") specifically -- not to `findLocal16PlusProvision` ("16+ provision in this LA"), since that second list answers a real membership question ("does this LA have 16+ provision at all") that an adult-only college honestly still answers "yes" to; narrowing it there would silently hide a real institution from a landscape list it genuinely belongs on. Separately, found and fixed a real, concrete match for the "HE etc." fragment while reading the code: `resolveSchoolTypeCategory` used its own ad-hoc, wider establishment-type list that also caught "Higher education institutions", "Miscellaneous" and "Welsh establishment" -- inconsistent with typology.ts's own canonical `FE_PARTICIPATION_ESTABLISHMENT_TYPES`, which deliberately excludes exactly those three because they have no real ILR crosswalk data at all. Classifying one of them as `fe_college` promised default lists that could never actually have real data -- aligned to the one real, already-verified list instead. Could not recover what "la swap" refers to from the corrupted note at all -- if this points at something else (a genuine Local-Authority attribution bug on the map, distinct from anything found above), please re-describe it and I'll pick it up as its own item.

---

## UX refinements round 1 (2026-09-06): B2 -- Special Schools closed out properly

Closed out the "Special Schools has no row in brief §6's table" gap flagged as an open decision in the previous build round. `classifySchoolTypeCategory` already recognised "special" as its own `SchoolTypeCategory`, but nothing downstream actually used it -- `inLaAllSectorsList` (List 2, "in [LA] all sectors") explicitly excluded the `"Special schools"` establishment_type_group from its candidate query, so a Special School target's own List 2 silently had zero special-school peers, and a mainstream target's List 2 never included a nearby special school either. Fixed by adding `"Special schools"` to that query's allow-list -- matches the public map's own treatment (`schools-in-bounds/route.ts` already fetches Special Schools as a real fourth sector alongside State/Independent/FE, not hidden or folded into State by default).

**Real, pre-existing bug found and fixed while confirming this looks right on the map**: `MapView.tsx`'s Sector colour-mode read `getComputedStyle` on the wrong DOM element (a parent of the element actually carrying the `--tag-*` CSS custom properties) -- custom properties cascade down to descendants, never up to ancestors, so every sector's dot silently rendered the grey fallback regardless of real sector, not just Special Schools. This predates the current task (introduced in the original Map v1 build, already flagged as "noticed, not fixed" in that round's own log entry) but is fixed now since B2 specifically depends on Special Schools' real red actually being visible to verify against. Fix: moved the ref onto the element that actually has the class carrying those properties.

---

## UX refinements round 1 (2026-09-06): A1 -- school name above the topic breadcrumb

Added as its own line directly above the topic tab row, sharing that row's own `px-4 sm:px-6` horizontal padding so it lines up with "Phase" in the filter row below it. The tab row itself gained the same padding in the same change -- it previously had none at all (flush against the very left edge, not aligned with "VicData" in NavBar.tsx above it), a real if minor pre-existing misalignment this also fixes.

---

## UX refinements round 1 (2026-09-06): A2 -- relevant filters, date range, Saved Sets, FE age filter, inline collapse

**Relevant-filters-only, real bug caught mid-build**: the first version of `relevantAgeBandsFor()` gated "Post 16" on the target's own effective phase TAGS (`phase.includes("Post 16")`) -- which, checked against real data, never shows for an ordinary through-to-18 school like Acland Burghley at all. `typology.ts`'s own `phaseTags()` deliberately never puts "Senior" and "Post 16" in the same tag set for one school (a considered, real-data-verified decision from an earlier round, not an oversight -- ages 16-18 fold INTO "Senior" for a through-school). But the request's own example explicitly expects BOTH "Senior" and "Post 16" to show for Acland Burghley -- checked directly against real data (11-18, phase = `["Senior"]` only) before shipping the first version, which would have silently failed that exact example. Fixed: "Post 16" is now shown whenever the target's real `statutoryHighAge >= 16`, independent of the categorical phase-tag check used for Junior/Prep/Senior -- the underlying age slice `[16, highAge]` is still real and worth isolating even though it isn't its own top-level category for a through-school. Verified again after the fix: Acland Burghley and Woldingham both correctly show Senior + Post 16; City Lit correctly shows only U19 (its relabelled Post-16) + Adult.

**Date-range control**: `DataViewFilterState` gained a `startPeriod` field (default 2019, matching the previous fixed anchor exactly). Rather than rename the ~6 files' worth of `anchor2019`/`ageGenderCounts2019`/`shape2019` identifiers throughout the codebase to something period-agnostic, the fixed-2019 fields were kept as literal, still-real defaults, and a new parallel, general path was added alongside them: `ageGenderCountsByPeriod: Map<period, AgeGenderCounts>` (data-view-profiles.ts, computed from already-fetched facts, no extra round-trip) and `profileToFilterableDataForPeriod(profile, period)` (data-view-serialize.ts), used by every "since [year]" trend badge, the Roll Trend chart's own plotted window, and the Map's trend colour-by mode. Logged as a deliberate scope trim: a full rename would touch significantly more surface for a purely cosmetic identifier change, since the actual VALUE these fields hold already defaults to the exact same 2019 anchor.

**Saved Sets + Save set button, sharing B3's own mechanism (built now since A2 needs it first)**: `SetOption`'s `"saved"` variant gained an optional `filters` snapshot (wire-safe form, `data-view-filters.ts`'s new `serializeFilterState`/`deserializeFilterState`), stored in `saved_sets.config` (a jsonb column that already existed, previously always written as the schema default `{}` by every save path). Recalling a saved set now restores both the school list (unchanged, via `saved_set_members`, same as before) AND the filter state it was saved with. The new `SavedSetsControl.tsx` (rendered inside `FilterBar`'s own row via an `extra` slot, not a second visually separate row) saves the CURRENTLY TICKED schools specifically -- the tick-list is this whole build's one real comparison mechanism (`ComparatorSidebar.tsx`'s own module comment) -- through the exact same `saved_sets`/`saved_set_members` tables and RLS policies `/sets/comparator/new/page.tsx`'s pre-existing save flow already uses, including the same personal-set cap (3, DB-trigger-enforced) surfacing the same real error message. Deliberately kept separate from the sidebar's own existing set-picker dropdown (which still lists recipe lists + saved sets together, unchanged) -- the sidebar answers "which schools am I comparing against," this new control answers "recall a whole bookmarked schools+filters combination," a genuinely complementary question once B3's own richer set-construction UI exists.

**FE age filter (U19/Adult)**: mostly built as part of B1 (the `feParticipation` compute path in `filteredCount`, and `"Adult"` as a new `PhaseBandKey`) -- A2's own remaining piece was purely the UI: `relevantAgeBandsFor` hides Junior/Prep/Senior/Early Years and shows U19 (Post 16, relabelled)/Adult instead whenever the target has real `feParticipation`, and `FilterBar.tsx` hides the age drill-down and Boarding pills entirely for such a target (no real per-age or boarding data exists for an FE college to slice by).

**Inline collapse arrow**: replaced the separate "Collapse filters"/"Show filters" text link below the filter row with `MapBoxCollapseToggle` (the same chevron component already used for the Map's own overlay boxes) rendered at the end of `FilterBar`'s own row -- always present regardless of collapsed state, so there's always something in the row to click to get back, matching the request's "inline in the same row as the filter choices themselves."

**Not yet verified live** -- built and checked against real data via direct script calls (`relevantAgeBandsFor`, `filteredCount`'s new FE branch), but the full UI (date-range control rendering, Saved Sets save/recall round-trip through a real click, the inline arrow's collapse behaviour) still needs a real Playwright pass, planned for the end of this round alongside B3/A3/B4.

---

## UX refinements round 1 (2026-09-06): B3 -- "Compared with" rework

**Renamed** everywhere it appears in the Data View's own UI (sidebar header, print-export summary line, an error message) -- the component file itself stays `ComparatorSidebar.tsx` (an internal identifier, not user-facing text) rather than a file rename purely for cosmetic internal-naming symmetry. The separate, pre-existing standalone `/sets/comparator/new` builder page (a distinct member-area tool, not part of the Data View) was deliberately left untouched -- out of scope for this round, which is about the Data View's own "Compared with" sidebar specifically.

**Select all + multi-LA, real decision on "adjacent"**: this repo has no real geographic LA-boundary-adjacency dataset at all (checked directly -- grepped the whole codebase for "adjacent"/"neighbour"/"bordering" before writing anything, found nothing that means geographic LA adjacency). Rather than fabricate a distance-threshold approximation, "adjacent" is derived from real data already available: the target's own nearest ~60 real schools' actual `la_name` values (added `laName` to `MatchedSchool`, one extra column on an already-running query, no new round-trip), deduplicated, in nearest-first order, capped at 8 candidates. Verified against real data before calling this good enough: Acland Burghley (Camden) → Islington, Haringey, Westminster, Hackney, Barnet -- the first two are the EXACT two boroughs A3's own example sentence names ("compared with schools and FE colleges in Camden, Islington and Haringey"), a strong real-data confirmation this proxy is doing the right thing despite not being a formal boundary dataset.

`buildLaComparatorSet` generalises the original single-LA `inLaAllSectorsList` to accept an array of LA names (same real phase/gender/roll-data-exists filtering, one implementation, not two) -- verified live: Camden+Islington+Haringey together return 30 real schools (hitting the existing LIST2_CAP, itself unchanged).

**Region/Nation/"same academy group"**: built as real, visible entries in the Local Authorities section, all three unconditionally disabled with an explanatory tooltip, exactly as the request asked ("build the UI entries now but grey them out/disable until the underlying data infrastructure to compute those sets actually exists. Don't wire fake data behind them"). "Same academy/school group" specifically: confirmed by querying the real `schools` table's own columns that there is NO trust/group/federation field anywhere in this schema at all -- not even a partial or stale one to build on. Given this is structurally identical to the Region/Nation gap the request itself already named as "build now, disable until real," the same treatment was applied here without waiting to ask, per the session's standing "make the most reasonable call and log it" instruction -- logged here explicitly since it wasn't itself named as uncertain in the request the way Region/Nation was.

**Nearest-10 "+5 more"**: `findSurroundingSchools` and `findNearestFeColleges` both gained an optional `targetCount` parameter (default-preserving -- every existing caller, including the free-tier public aggregate, is completely unaffected). The button re-runs the SAME real matching pipeline at a wider count rather than "load 5 more and append," so the result stays correctly nearest-first ordered at every size. Deliberately NOT routed through the existing `selectSet()` (which pre-ticks only the first `INITIAL_TICKED_COUNT` and would silently discard whatever the member had already manually ticked/unticked) -- a new `expandActiveSet()` grows the active set's school list in place and auto-ticks ONLY the newly-appeared schools, leaving every existing one's ticked state exactly as the member left it. Verified live: Nearest 15 for Acland Burghley returned 14 real schools (an honest under-fill, not a bug -- the same "may return fewer than requested when the filtered pool runs thin" convention this codebase already applies everywhere else).

**Not yet verified live in the browser** -- the underlying data functions and API routes are confirmed against real data via direct script calls; the full UI (checkbox interactions, the Select all button, the disabled placeholder rows, the +5 button's live re-render) still needs a real Playwright pass, planned for the end of this round.

---

## UX refinements round 1 (2026-09-06): A3 -- dynamic filter-summary sentence

Built as a genuine template system (`data-view-summary.ts`, no server-only imports, recomputed fresh every render from the live filters/activeSet/target) rather than a lookup table keyed to Guy's own five illustrative examples -- those describe the SHAPE of sentence wanted, not five literal strings to special-case.

**Verified against real data, reproducing two of the five examples exactly (structurally) and the rest closely**:
- "Total roll 2019/20 to 2025/26 compared with 9 nearest state senior schools" for Acland Burghley's real Nearest-10 set, no filters -- matches example 1's shape exactly (real N=9, not 10, because that's genuinely how many real phase/gender/roll-matching candidates exist for it right now -- an honest reflection of real data, not a bug: `nearest_schools`' own RPC restricts candidates to the target's own sector AND phase by construction, confirmed directly from that module's own header comment, which is *why* "N nearest state senior schools" is always literally true of this recipe's real membership rather than a guessed adjective).
- "Girls roll 2019/20 to 2025/26 compared with all senior schools in Camden" for Girls-filtered + the real single-LA set -- an exact structural match to example 2.
- "Sixth form roll 2019/20 to 2025/26 compared with all senior schools in Camden, Islington and Haringey" for Post-16-filtered + the real 3-LA set built for B3 -- very close to example 3, except it says "all senior schools" rather than "schools and FE colleges": the current multi-LA set (`buildLaComparatorSet`) deliberately doesn't include FE colleges (B3's own bullets never asked for a cross-sector LA set, only single-sector "all sectors" meaning state+independent+special) -- logged as a genuine gap between this one illustrative example and what B3 was actually asked to build, not silently smoothed over. Closing it for real would mean a new LA-set variant that also pulls in FE colleges (similar in spirit to `findLocal16PlusProvision`'s own cross-sector logic, but generalised to multiple LAs) -- real, additional work, not done this round absent an explicit ask for it.

**Deliberate wording choice**: date ranges render as "2019/20" (this codebase's own established academic-year-label convention, used verbatim elsewhere -- RollCard.tsx/FeCollegeCards.tsx/SmallCards.tsx/FilterBar.tsx) rather than Guy's own prose's "2019-20" hyphen -- consistency with the rest of the app over matching illustrative prose literally.

**Not yet verified live in the browser** -- confirmed via direct script calls against real profiles/lists; a real Playwright pass (does the line visibly update the instant a filter pill is clicked) is still to follow, planned for the end of this round alongside B4.

---

## UX refinements round 1 (2026-09-06): B4 -- map dot interaction

**Bare dots until added**: a school not yet in the "Compared with" tick-list now renders as a small (3.5px, deliberately below the smallest real data-driven radius of 5px, so it reads as "not sized by data at all" rather than "the smallest real value happens to land here"), flat neutral-grey dot with a hover tooltip showing ONLY its name + "click to add to comparison" -- no count, no trend, no colour-by-sector. This replaces the previous "faded but partially visible" state, which was real data (real size, real colour, real trend %) just dimmed via opacity -- honestly still real data, not "no data," which is what B4 asked to remove. The moment a school is ticked (or is the target), it gets the exact same full real-data treatment as before, unchanged.

**"Turning off a filter/selector must hide the corresponding dots... confirm this isn't already silently broken" -- checked directly, and it was**: a school with a genuinely zero real count for the active phase/gender/boarding filter (an all-boys school under a "Girls" filter, say) still rendered a same-shaped dot as every other school, which reads as "this school has some of what's being filtered for" when it honestly has none. Fixed: any school (ticked or not) with a real filtered count of exactly 0, while at least one narrowing filter is active, is now skipped entirely rather than drawn -- the target is the one exception, never hidden, matching the "the viewed school is always a real reference point" rule already applied everywhere else in this build. Map bounds/fit stay inclusive of every real position regardless of this hide, so the viewport doesn't jump around as filters change.

**Not yet verified live in the browser** -- confirmed the logic and constants directly by reading the rewritten marker loop; a real Playwright pass (toggling a filter that zeroes out a real school, ticking/unticking a bare dot, confirming the tooltip content genuinely differs before/after) is next, alongside the rest of this round's live verification.

---

## UX refinements round 2 (2026-09-07): P1 -- Local Authorities picker bugs

**Root cause, confirmed live before fixing (reproduced first, not guessed)**: the LA picker's `applyLaSelection` tagged every result `key: "multi_la"` -- a key that never exists in the sidebar dropdown's own `options` array (that array only ever holds the server's fixed recipe/saved sets, e.g. `"in_la"` for the original single-LA default). So the `<select>`'s `value` never matched any real `<option>`, and the browser silently fell back to displaying whichever option happened to be listed first -- reproduced directly: toggling Camden's own checkbox genuinely updated the map (dots went 11→7 for a real Camden-only result, confirmed via a direct API call and DOM inspection) while the dropdown's own label stayed frozen on "Nearest 10 (any LA)". Fixed by having the dropdown inject the CURRENTLY ACTIVE set as a synthetic extra `<option>` whenever it isn't already one of the real ones, so the displayed label is always honestly in sync with whatever's actually active.

**A second, related bug found while fixing the first**: `applyLaSelection` had no guard at all against out-of-order responses -- two overlapping LA toggles (nothing stopped a member clicking a second checkbox before the first fetch resolved) could let a slower, staler response overwrite a newer one. This is what "dots don't appear consistently for Camden" looks like from the outside. Fixed with a simple request-sequence counter -- a response is only applied if it's still the most recent request in flight.

**Item 2 (LA selector shown for non-LA-scoped sets)**: the whole "Local Authorities" panel (checklist + Region/Nation/group placeholders, all nested inside it) is now shown only when the active set genuinely IS LA-scoped (`key === "in_la"` or `"multi_la"`) -- verified live: hidden under "Nearest 10 (any LA)", appears the moment "In Camden (all sectors)" (the pre-existing dropdown entry) is selected. Reached the first time via that ordinary dropdown entry, not circular -- Region/Nation/group become visible only in that same LA-scoped context now, a minor side effect of gating the whole panel as one unit rather than splitting it, logged here since it wasn't itself named as a problem in the request.

**Item 3 (Boarding filter shown despite zero boarders)**: extracted the exact check `DashboardView.tsx`'s own `targetIsDaySchool` already used inline (`hasRealBoardingProvision`, `data-view-filters.ts`) and used it to gate the Boarding pill group in `FilterBar.tsx` too -- Acland Burghley (a real day school) no longer shows a Boarding filter that could only ever produce the same whole-school number either way it's clicked. Both call sites now share the one real check, so they can't drift apart on what "doesn't board" means.

All three verified live with Playwright against real data (Acland Burghley) before moving on.

---

## UX refinements round 2 (2026-09-07): P2 item 4 -- topic tabs redesign

School name (bold, larger, dark) now shares one line with the topic tabs, to their left. Tabs are flat pills, no chevrons (navigation between four independent topics, not a sequence). Each topic has a fixed identity colour (`TOPIC_COLOURS`, `tag-colours.ts`) applied everywhere it appears -- including PDF export headers, which this row already reaches for free since it isn't `print:hidden` (window.print() captures the live page; there's no separate PDF template in this codebase to duplicate the colour into).

**Logged, not silently smoothed over**: finding four colours genuinely distinguishable from every one of the 15 existing school-typology tag colours AND from each other turned out not to be achievable -- this palette is already deep enough (checked directly) that no four fresh, legible hues remained fully clear of every existing entry. Optimised instead for the requirement that actually matters: the four TOPIC colours are clearly distinct from EACH OTHER, since tabs are the only place they appear together (blue/Rolls, purple/Academic, stone/Destinations, maroon/Context -- a deliberate spread across cool, neutral and warm rather than four hues from one family). Some proximity to existing tags is real (Rolls' blue near "Through School"'s blue; Context's maroon near "Special Schools"' red) but those tags and these topic colours are never the thing being compared side by side, so it shouldn't cause real confusion in practice -- flagged here in case it reads differently once seen live.

**Per the request's own explicit instruction**, the active topic tab is ALWAYS shown filled (this is navigation, not filtering -- there's no "default tab" to narrow away from) -- deliberately NOT the same "colour only when narrowing from all" rule P2 item 5 applies to actual filter pills, kept as two visually similar but conceptually separate conventions (separate CSS class names, `topic-tab-active` vs `filter-pill-active`) so the two systems can't accidentally merge into one and blur the distinction later.

---

## UX refinements round 2 (2026-09-07): P2 item 5 -- filter colour convention

Confirmed directly by reading `MapFilterPanel.tsx` (the public map's own filter panel) before building anything: its established convention is inactive = plain neutral border/text, active = fills with the VALUE's own real `TAG_COLOURS` colour (the same palette `TypologyTags.tsx`/`SchoolMap.tsx` already use for these exact tags) -- not a generic dark fill. Reused verbatim, including its exact dark-mode technique (a `--pill-bg-dark`/`--pill-fg-dark` custom-property pair plus a shared `.filter-pill-active` class with `!important` overrides, since an inline style can't react to a `prefers-color-scheme`/`data-theme` change on its own).

Applied to every real filter pill this round: Phase/age bands, Gender, Boarding (mapped to TAG_COLOURS' "Boarding"/"Day" keys, since the filter's own labels "Boarders"/"Day pupils" read better as UI text but don't match those keys literally), and the new Sector filter. The per-age drill-down buttons ("Ages: 16 17 18...") were deliberately left with their existing plain black/white active style -- there's no real per-age colour anywhere in this app's palette to reuse, and inventing one for individual integers didn't seem like what "same convention as the rest" was asking for; flagged here in case that reading's wrong.

Two new palette entries were needed and didn't exist before this round: "Early Years" (yellow) and "Adult" (slate, deliberately desaturated -- reads as "the generic option" next to U19's own orange, which is Post 16's colour reused verbatim since U19 is the same underlying filter value, just relabelled for an FE audience).

---

## UX refinements round 2 (2026-09-07): P2 item 6 -- map auto-fit and scroll-to-zoom

**Auto-fit to ticked schools**: the map's own `fitBounds` call already existed and already recalculated on every relevant state change (it was already a real, working mechanism) -- what was wrong was WHICH schools it included: every real position in the active set regardless of tick state, a holdover from before per-school hide/show existed. Narrowed to only the schools actually ticked (+ the target, always) -- verified live by watching the real bounds array shrink (10 → 9 → 8 → ... → 2) as schools were unticked one at a time, each one correctly triggering a fresh `fitBounds` call with the smaller set.

**Cmd/Ctrl+scroll-to-zoom**: built with `leaflet-gesture-handling` (MIT, zero dependencies, exactly the "Prompt desktop users to use Ctrl+Mouse Wheel to zoom... Google Maps gesture handling" plugin the request itself named) rather than a hand-rolled equivalent. One real integration wrinkle: it's an old-style Leaflet plugin that expects a bare global `L` (confirmed by reading its own source -- no `require`/import of Leaflet at all, "deps: none" per its own package metadata), which a bundler's scoped ES module import doesn't provide for free -- worked around by setting `window.L` to the same Leaflet module instance immediately after loading it, before the plugin's own side-effect import runs. Scoped to the Data View's own map only (`gestureHandling: true` on `MapView.tsx`'s map instance) -- the public map (`SchoolMap.tsx`) was deliberately left untouched, since this request was about the Data View specifically, not a site-wide behaviour change to a page members already know how to use. Verified live: a bare scroll over the map correctly triggers the plugin's own warning-hint class without changing zoom.

**Incidental fix, not app-related**: installing this package via `npm install` pruned an existing, undeclared local Playwright install (never in package.json -- npm's default `install <pkg>` behaviour reconciles the whole node_modules tree against the lockfile, not just adds the new package). Restored with `npm install --no-save playwright` afterward, confirmed via `git diff` that neither package.json nor package-lock.json carry any trace of it -- flagging this here only for transparency about a real, if fully-reverted, side effect during this round's own work, not because it affects the shipped app in any way.

---

## UX refinements round 2 (2026-09-07): P3 item 7 -- Sector filter

A genuine membership filter, not a slice -- logged directly in `data-view-filters.ts`'s own updated module comment, since this is a real, deliberate exception to the "filters slice the number, they never change set membership" rule every other filter in this build has followed since round 1. A school's sector is a whole-school category, not a per-pupil number with a piece to narrow, so the only honest behaviour an active sector filter can have is excluding non-matching schools entirely from Map/Dashboard/Rankings -- deliberately reusing the PUBLIC map's own established sector-filter convention (hide/show) rather than inventing a different mechanism for this one field.

Relevance gate: only shown (and only pills for sectors actually present, not all four unconditionally) when the active set genuinely spans more than one real sector -- computed across the whole active set (ticked or not), target included. Verified live: absent under "Nearest 10 (any LA)" (single-sector by construction, per `nearest_schools`' own sector-equality gate), appears with real Independent/State/Special Schools pills under "In Camden (all sectors)", and activating "Independent" correctly fills the pill with the real ISC orange and removes every non-Independent dot from the map while leaving the target (State) visibly unaffected.

---

## UX refinements round 2 (2026-09-07): P3 item 8 -- hide all compared schools

**Flagged by Guy himself as needing confirmation rather than an assumption** -- built to his own stated reading exactly as given: a single toggle ("Hide compared schools" / "Show compared schools", sidebar) that temporarily suppresses every ticked school's data from Map/Dashboard/Rankings WITHOUT touching `tickedUrns` (set membership) at all -- distinct from item 9's Select all/Unselect all, which does change membership. Verified live: toggling hide collapses the Dashboard's spread strips to just the target (market share reads 100%) while every sidebar checkbox stays checked; toggling show restores the exact same 9-school comparison with no re-selection needed. **Logging this as the operating interpretation per his own instruction** -- if "hide all" was meant to mean something else (e.g. a per-school hide distinct from unticking, or something scoped only to the Map rather than all three views), please say so and it'll be adjusted; the reading above is what's shipped.

---

## UX refinements round 2 (2026-09-07): P3 item 9 -- Select all / Unselect all

Added next to the sidebar's schools list, operating on `tickedUrns` (real membership, unlike item 8 above) -- "Select all" ticks every school currently in the active set's full list (visible + overflow together), "Unselect all" clears every tick. Verified live: both work correctly and independently of the "Hide compared schools" toggle.

---

## UX refinements round 2 (2026-09-07): P4 -- Local Authorities arrow size

Was a bare unicode ▾/▸ glyph, visibly smaller than every other open/close control in the shell. Replaced with `MapBoxCollapseToggle` -- the exact same chevron component `FilterBar.tsx`'s own collapse arrow already uses -- so there's one consistent arrow style across the shell rather than several ad hoc ones. Rendered as a sibling of the "Local Authorities" text label (not nested inside one button with it, since `MapBoxCollapseToggle` renders its own button and two interactive elements can't nest) -- both still toggle the same state, so the click target is, if anything, larger than before.

---

## Bug fixes (2026-09-08): FE colleges missing from Post-16 comparisons; "Hide compared schools" mis-spec

**Bug 1 -- FE colleges never appeared as real candidates for a mainstream Post-16 target**: root cause confirmed by reading the code directly, not guessed. `findLocal16PlusProvision` (`surrounding-fe-colleges.ts`) already existed and already did exactly the right real-data query (FE colleges + genuine sixth-form schools in the target's own LA) -- but it was only ever CALLED for an `fe_college`-category target (that branch's own List 2, "16+ provision in {LA}"). A mainstream school with a real sixth form (Acland Burghley, 11-18) had no recipe offering FE colleges at all -- the Post-16 summary sentence's own "compared with schools and FE colleges in..." could only ever become true if a member manually searched for and added an FE college themselves. Fixed by adding a new `local16Plus` field to `DefaultComparatorLists`, populated for any mainstream target with real Post-16 provision (`statutoryHighAge >= 16`, the same real check `relevantAgeBandsFor` already uses for the Post-16 filter pill itself), reusing `findLocal16PlusProvision` unchanged. Also added a specific `data-view-summary.ts` case so this new recipe's own sentence reproduces the brief's own example 3 close to verbatim ("...compared with schools and FE colleges in Camden"). Verified live: selecting the new "Schools and FE colleges, 16+, in Camden" recipe for Acland Burghley shows WM College (a real FE college) in the sidebar tick-list.

**Bug 2 -- "Hide/Show compared schools" (round 2, P3 item 8) was a real misreading**: it hid schools' DATA from Map/Dashboard/Rankings; what was actually meant was collapsing the SCHOOLS LIST itself inside the "Compared with" sidebar box, since a long list was pushing the sidebar's height too tall and (since the sidebar and map share one row) dragging the map's own height down with it. Added a proper accordion collapse on the schools list, reusing `MapBoxCollapseToggle` (the same arrow the filter row and Local Authorities panel already standardised on), collapsed state showing just a real count ("Schools (9)"). Verified live: collapsing hides the target-name line, Select-all/Unselect-all, and the full tick-list, leaving just the count + arrow -- a dramatic height reduction for a real 9-school list.

**Kept the original map-hide toggle, per Guy's own explicit "your call, log it" instruction**: it's a genuinely separate, legitimate feature (temporarily hiding comparison DATA from Map/Dashboard/Rankings without touching set membership) -- not redundant with the new list-collapse (a pure sidebar-height/layout concern with no effect on what the other views show). Cheap to leave in, so it stayed, but renamed from "Hide/Show compared schools" to "Hide/Show comparison data" -- the original wording would have been actively confusing sitting directly under a new "Schools (N)" accordion header describing a completely different kind of collapse.
